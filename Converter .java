public class Converter {
        //Считаем калории
        int calories(){
            totalCalories = (allSteps * 50)/1000;
    
        }return totalCalories;
        //Считаем км
        double totalDistance(double distance){
            distance = (allSteps*0.75)/1000;
        }return distance;
    
        
}
